# Copia este archivo a .env y cambia los valores

# Contraseña de la app (la misma que usas en la pantalla de bloqueo)
API_TOKEN=mi_password_secreta

# Puerto donde se expone la app (el que apuntas en Cloudflare Tunnel)
PORT=3000
